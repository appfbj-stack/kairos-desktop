/**
 * System tray - icone na bandeja do Windows com menu.
 * Stub - implementacao real na Fase 3.
 */

import type { BrowserWindow } from 'electron';
import { logger } from './logger.js';

export function setupTray(_win: BrowserWindow): void {
  logger.info('System tray setup (TBD Fase 3)');
  // TODO Fase 3: Tray icon + menu (Show/Hide, Settings, Quit)
}
